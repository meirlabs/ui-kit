# Drain review screenshots

## LAB-1 — StatusPill motion

Preview screenshots of the StatusPill motion demo, captured for this drain PR.
Static PNGs cannot show the motion itself (breathing dot + entrance settle) — run
the preview app (`pnpm preview` → Primitives → Status Pill) to see it live.

## LAB-4 — OnboardingWidget

Preview screenshots of OnboardingWidget (default / all-complete / minimal variants),
light + dark. Captured from the ui-kit preview app (Data Display → Onboarding Widget).
Dark card surface resolves to the dark `--ml-*` tokens (verified: rgb(28,33,41),
root color-scheme dark).

These files are review artifacts only. Drop the `.drain-review/` folder on merge.
